# Local media

Development images live here so the site does not depend on image APIs.

Add a licensed audio file named `yellow.mp3` to this folder when you have the rights to use it. The app already contains the music controls and will attempt to start the track after the first YES interaction.

Replace `dummy-cat.svg` later with the real anniversary photos, keeping the same paths or updating the content manifest when we build the admin/backend layer.
